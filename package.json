{
  "name": "tone-practice",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mandarin tone and tone-pair listening drills (traditional characters)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:decks": "tsx scripts/build-decks.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "jsdom": "^25.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
